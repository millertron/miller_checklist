<h1>Edit Checklist for <%=@user.first_name%> <%=@user.last_name%></h1>
<div>
	<%= form_for(@checklist) do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name %>
		<br/>
		<%= f.label :description%>
		<%= f.text_area :description %>
		<br/>
		<%= f.submit %>
	<% end %>
</div>